[package]
name = "cachecow"
version = "0.1.0"
edition = "2024"
description = "A stupid simple JSON key-value filesystem cache."
license = "MIT"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
thiserror = "2"

[dev-dependencies]
user_dirs = "0.2.0"
